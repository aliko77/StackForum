import AdminPanel from 'layouts/AdminPanel';
import { FC } from 'react';

const Admin: FC = () => {
   return (
      <>
         <AdminPanel>
            <div>Admin</div>
         </AdminPanel>
      </>
   );
};

export default Admin;
