// TODO
class TokenService {
   getCookieRefreshToken() {
      const refreshToken = '';
      return refreshToken;
   }

   getCookieAccessToken() {
      const accessToken = '';
      return accessToken;
   }
}

export default new TokenService();
